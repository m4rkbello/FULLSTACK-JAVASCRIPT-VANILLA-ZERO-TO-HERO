//initializer
let personName = prompt("What is your name?");

//naay isa ka variable parser tapos gisulod sa argument if/? tama ang return ang mo gawas is 10 or else/: ang return is 0;
let data = personName === "MARKBELLO" ? 10 : 0;

//console.log gamit backtick
console.log("YOUR HAVE", `${data}`);